{
  "name": "startup-role-search",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.6"
  },
  "scripts": {
    "start": "node src/server.ts",
    "dev": "node --watch src/server.ts",
    "typecheck": "tsc --noEmit",
    "init-storage": "node src/init-storage.ts"
  },
  "dependencies": {
    "express": "^5.1.0",
    "google-auth-library": "^10.1.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.3",
    "@types/node": "^24.0.10",
    "typescript": "^5.8.3"
  }
}
